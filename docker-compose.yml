name: home-gate

services:
  phoenixd:
    container_name: phoenixd
    image: acinq/phoenixd:latest
    ports:
      - "9740:9740"
    volumes:
      - "./phoenixd:/phoenix/.phoenix"
    restart: unless-stopped

  backend:
    container_name: backend
    build:
      context: ./back-end
      dockerfile: Dockerfile
    ports:
      - "8881:8881"
    depends_on:
      - phoenixd
    volumes:
      - ./back-end:/app
      - /app/node_modules
      - /app/dist
    restart: unless-stopped
    env_file:
      - ./back-end/.env

  frontend:
    container_name: frontend
    build:
      context: ./front-end
      dockerfile: Dockerfile
    ports:
      - "8882:8882"
    depends_on:
      - backend
      - phoenixd
    volumes:
      - ./front-end:/app
      - /app/node_modules
      - /app/.next
    restart: unless-stopped
    environment:
      - NODE_ENV=development
      - PORT=8882
      - NEXT_PUBLIC_BACKEND_URL=https://api.homegate.live
      - NEXT_PUBLIC_WS_URL=wss://api.homegate.live/ws

